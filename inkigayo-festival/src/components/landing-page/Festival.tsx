import styles from '@/styles/components/landing-page/Festival.module.css'
export default function Festival() {
  return (
      <section id='o_festival' className={styles.container}>
        Festival
      </section>
  )
}
